// these d.ts files are just for defenition
// They are super convienient as we don;t have to import them anywhere to use them

interface User{
    name:string,
    email:string,
    image:string,
    id: string
}